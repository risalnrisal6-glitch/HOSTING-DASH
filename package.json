{
  "name": "nova-panel",
  "version": "1.0.0",
  "private": true,
  "description": "NOVA PANEL - Premium Pterodactyl hosting dashboard",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n API,WEB -c magenta,cyan \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "setup": "npm install && npm run db:push -w apps/api && npm run db:seed -w apps/api && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
